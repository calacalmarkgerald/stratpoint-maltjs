// import { selectBaseContainerDomain } from '../selectors';

describe('selectBaseContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
