/*
 * BaseContainer Messages
 *
 * This contains all the text for the BaseContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BaseContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BaseContainer container!',
  },
});
