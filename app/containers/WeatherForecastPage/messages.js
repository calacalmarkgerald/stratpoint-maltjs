/*
 * WeatherForecastPage Messages
 *
 * This contains all the text for the WeatherForecastPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WeatherForecastPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WeatherForecastPage container!',
  },
});
