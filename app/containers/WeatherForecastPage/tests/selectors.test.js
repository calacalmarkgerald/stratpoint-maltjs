// import { selectWeatherForecastPageDomain } from '../selectors';

describe('selectWeatherForecastPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
